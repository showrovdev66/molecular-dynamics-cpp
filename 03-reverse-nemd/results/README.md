# Generated results

Run the NEMD program from the `03-reverse-nemd` folder. After the simulation finishes, this folder will contain:

- `temperature_profile.csv` - average reduced temperature in each slab;
- `nemd_summary.txt` - measured thermal conductivity and validation values.

These files are generated by the C++ program, so their numerical values depend on the run and compiler/platform details.
